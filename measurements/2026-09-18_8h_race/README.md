# 2026-09-17/18 대회 조건 8h (WiFi·WWAN 끔, PackageKit·snapd·타이머 정지) — **네트워크 가설 기각**

## 조건
- 운용 설정(rt-poll + eco 800 + hk ondemand), `soak_guard on race` 20:12:59 적용 확인(저널: rfkill wifi/wwan block, apt 타이머 정지, snapd·PackageKit 정지). 측정 20:13:25 ~ 04:13:25, 무인. 측정 중 rtw LPS 메시지 0건(어댑터 꺼짐).
- 부하·격리·MSR 기록 동일. 패키지 41~61 °C, GPU 85 %, SMI 0, 열 스로틀 0, 커널 경고 0.

## 결과 — `soak_20260917_201318.*`

| | 9/16 8h (WiFi 켬, 서비스 켬, 화면 잠김) | **9/17 8h 대회 조건 (WiFi 끔, 서비스 끔, 화면 켜짐)** |
|---|---|---|
| 최악 | 920 µs | **889 µs** |
| 50 µs 초과 | 81 | **1,946** |
| 200 µs 초과 | 31 | **1,478** |
| 400 µs 초과 | 26 | **1,181** |
| 에피소드 | 3 | **70** (8코어 동시형 다수, 최대 222건짜리 군집) |
| 시간대별 에피소드 | 00:07, 06:12 | 0h:6 1h:19 2h:2 3h:8 4h:11 5h:2 6h:11 7h:11 — **첫 1시간부터** |

- 에피소드 순간의 열 로그: 격리 800 MHz, 하우스키핑 2.2~2.6 GHz, 35.0 W, `limit=PL1` 만 — **전력·클럭 제한 사유에 특이점 없음.** GPU 사용률 급락과의 겹침 70개 중 2개(무관).
- 아침 12:24 (재부팅 없이, uptime 17 h) 3분 + provoke: 최악 770, 400 초과 6 → 스톨 상태 지속.

## 판정
1. **"네트워크·패키지 활동이 원인"은 기각.** WiFi·서비스를 전부 끈 8h 가 켠 8h 보다 23배 나빴다. 9/17 14:08 의 130개는 네트워크가 만든 것이 아니라 **이미 스톨 상태인 머신에서** 네트워크가 증폭한 것(그 판정은 유지), 그리고 스톨 상태의 유무가 실행마다 달랐던 것이다.
2. 스톨 상태의 on/off 를 결정하는 조건은 여전히 미확인. 실행별 조건 비교:

| 실행 | 결과 | uptime | 화면/세션 | 기타 |
|---|---|---|---|---|
| 9/16 21:32 30 min | 0 | 1 h | 켜짐(사용자 작업 중) | |
| 9/16 22:22 8h | 3 | 2~10 h | **잠김**(soak_guard 가 root 설정을 바꿔 사용자 화면은 5분 뒤 잠김) | WiFi 켬 |
| 9/17 14:08 30 min | 130 | 18 h | 켜짐(사용자 작업 중) | provoke |
| 9/17 16:00~19:44 3 min ×4 | 0 | 0~0.3 h | 켜짐 | 새 부팅 직후 |
| **9/17 20:13 8h** | **70** | 1~9 h | **켜짐(soak_guard 가 잠금 해제, 밤새 켜짐)** | WiFi 끔, **snapd-desktop-integration 1 Hz 오류 루프**(snapd 정지 부작용, 시간당 3,595 저널 줄) |
| 9/18 12:24 3 min | 6 | 17 h | 켜짐 | snapd 루프 지속 중 |

   - uptime 단독 아님(1 h 에 이미 발생 / 10 h 에 조용).
   - 남는 후보: (a) **GNOME 세션·디스플레이 상태**(화면 잠김/DPMS 꺼짐이던 유일한 8h 가 가장 조용) — NVIDIA 470 on RT 의 컴포지터 렌더링·스캔아웃 경로; (b) **snapd-desktop-integration 오류 루프**(이번 8h 에만 있었음; 14:08 엔 없었으므로 단독 원인은 아님); (c) 미상.
3. 커널 가시 경로(IRQ·IPI·SMI·ftrace 이벤트·MSR 제한 사유) 전부 특이점 없음은 그대로.

## 다음 — "지금 스톨 상태"를 이용해 **끄는 스위치**를 찾는다 (3분 × N, provoke 없이)
머신이 지금 스톨 상태(12:24 에 3분 6건)이므로, 조건을 하나씩 제거하며 0 이 되는 것을 찾는 편이 켜는 조건을 찾는 것보다 빠르다.

| # | 조치 | 판정 |
|---|---|---|
| S0 | 아무것도 안 바꾸고 3분 | 기준(≥5 건이면 상태 유지 확인) |
| S1 | `systemctl --user restart snapd-desktop-integration` 로 오류 루프 종료 → 3분 | 0 이면 (b) |
| S2 | 화면 잠금(`loginctl lock-session`) + DPMS 끔 상태에서 3분 | 0 이면 (a) 디스플레이/컴포지터 |
| S3 | USB WiFi 어댑터 물리 제거 → 3분 | 0 이면 USB 장치 |
| S4 | glmark2 없이(A1_NOLOAD 아님, GPU 부하만 제거) 3분 | 0 이면 GPU 부하 필요조건 |
| S5 | 재부팅(rt-poll) 직후 3분 | 0 이면 "새 부팅" 규칙 유효 재확인 |

## 파일
`soak_20260917_201318.*`(8h), `soak_20260918_122412.*` + `provoke_20260918_122418.log`(아침 3분)
