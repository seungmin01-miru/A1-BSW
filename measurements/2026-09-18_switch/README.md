# 2026-09-18 스톨 상태 "끄는 스위치" 탐색 (S0~S2, 3분씩, provoke 없음, 운용 설정)

전제: 이날 아침 12:24 3분에 400 µs 초과 6건 → 머신이 스톨 상태. 조건을 하나씩 제거하며 0 이 되는 것을 찾는다.

| # | 조치 | 시각 | 최악 | 50 / 200 / 400 µs 초과 | 판정 |
|---|---|---|---|---|---|
| S0 | 없음 (기준) | 12:34 | 322 µs | 69 / 11 / 0 | 스톨 상태(약) |
| S1 | `snapd-desktop-integration` 오류 루프 종료(`pkill`) | 12:43 | 482 µs | 12 / 5 / 1 | **여전히 스톨** → 루프는 원인 아님 |
| **S2** | **화면 잠금 + DPMS 끔** (`loginctl lock-session && xset dpms force off`) | 12:47 | **19 µs** | **0 / 0 / 0** | **스톨 소멸** |

- S2 는 전 실험 통틀어 가장 깨끗한 3분(스레드별 최악 ≤ 19 µs). glmark2 오프스크린 부하는 화면이 꺼진 동안에도 GPU 81 % 로 계속 돌았다 → **GPU 연산 부하가 아니라 디스플레이·컴포지터 경로**가 조건이다.
- 저널: 12:46:50 NVIDIA X 드라이버가 DPMS 전환으로 디스플레이 재프로브(DFP-0~5) — 화면 꺼짐이 실제로 일어남.

## 지금까지의 실행과 정합성
| 실행 | 디스플레이 | 결과 |
|---|---|---|
| 9/16 8h | **잠김**(soak_guard 버그 덕에 5분 뒤 잠김) | 3 에피소드 |
| 9/17 14:08 30 min | 켜짐, 사용자 작업 중 | 130 |
| 9/17 20:13 8h | **밤새 켜짐**(soak_guard 가 잠금 해제) | 70 |
| 9/18 12:24·12:34·12:43 3 min | 켜짐 | 6 / 0(200+ 11) / 1 |
| **9/18 12:47 3 min** | **잠김 + 꺼짐** | **0, 최악 19** |
| 9/17 16:00~19:44 3 min ×4 | 켜짐, 새 부팅 직후 | 0 — 예외. 새 부팅 직후엔 컴포지터/드라이버 상태가 아직 "스톨 상태"가 아니었다고 해석(가동 시간이 필요조건일 가능성 잔존) |

## 판정
- **디스플레이(NVIDIA 470 on RT 의 컴포지터 렌더링·스캔아웃 경로)가 활성일 때 격리 코어 전역 스톨이 난다.** 네트워크 사건은 이 상태의 증폭 인자였다. 기전(드라이버가 커널에 보이지 않는 경로로 8코어를 0.5~0.9 ms 세우는 방법)은 미확인 — NVIDIA 비공개 드라이버라 논문에선 "관찰"로 둔다.
- **운용 규칙**: 주행 중 **디스플레이 비활성**(화면 잠금+DPMS, 또는 GDM 없이 multi-user.target 으로 부팅) + 주행 전 새 부팅 + WiFi 끔(증폭 인자 제거) + 백그라운드 갱신 정지. `soak_guard on race` 를 이에 맞게 수정(60초 뒤 자동 잠금, snapd 는 정지하지 않음).
- 최종 검증 = 이 조건으로 8h 1회(무인, 밤) — Phase C 와 병행.
